/**
 * Public API facades and compatibility contracts for NeoOpenComputers.
 *
 * <p>The API is split into driver, file system, machine, manual, nanomachine,
 * and network packages. Addons generally use the top-level facade classes for
 * service lookup and the subpackages for implementation contracts.</p>
 */
package li.cil.oc.api;
